{
  "name": "kisi",
  "version": "1.0.0",
  "description": "Server and Client for image mosaic",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install && npm run install-packages",
    "install-packages": "concurrently \"cd functions && npm install\" \"cd frontend && npm install\"",
    "start": "concurrently \"cd functions && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build && cp -r build ../functions/public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ventsislav Antov",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
