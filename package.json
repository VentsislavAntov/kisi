{
  "name": "kisi",
  "version": "1.0.0",
  "description": "Server and Client for image mosaic",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install && npm run install-packages",
    "install-packages": "concurrently \"cd server && npm install\" \"cd frontend && npm install\"",
    "start": "concurrently \"cd server && npm run dev\" \"cd frontend && npm start\"",
    "build": "concurrently \"cd server && npm run build\" \"cd frontend && npm run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ventsislav Antov",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
